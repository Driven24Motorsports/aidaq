# aiDAQ — Python dependencies
# Install with:  pip install -r requirements.txt
#
# NOTE: pyirsdk is NOT listed here because it is only available on Windows
# with iRacing installed.  Install it separately on your local machine:
#   pip install pyirsdk
# The cloud deployment (Railway) does not need it.

# ── Core data processing ───────────────────────────────────────────────────
polars>=0.20.0          # fast DataFrame library (replaces pandas for large .ibt files)
pyarrow>=14.0.0         # required by polars for Parquet read/write
numpy>=1.24.0           # numerical operations (used in physics.py)

# ── Dash / Plotly UI ───────────────────────────────────────────────────────
dash>=2.14.0            # web app framework (includes dcc, html, callback)
dash-bootstrap-components>=1.5.0   # dark theme, layout grid, card components
plotly>=5.18.0          # interactive chart library

# ── Optional (pandas compatibility layer) ──────────────────────────────────
# Some older Plotly helpers expect pandas DataFrames.
# polars_df.to_pandas() converts on-demand when needed.
pandas>=2.0.0
